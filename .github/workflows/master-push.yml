name: Master branch push workflow

on:
  push:
    branches:
      - master

env:
  NODE_VERSION: 12.x
  AWS_ACCOUNT_ID: '123'
  REGION: eu-central-1
  ENVIRONMENT: stage
  TF_VERSION: 1.0.2
  DB_USER: ${{ secrets.DB_PASS }}

jobs:
  deploy:
    uses: jbialobr/gha-playground/.github/workflows/deploy.yml@master
